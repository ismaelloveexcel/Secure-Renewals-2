# Azure Environment Variables Template
# Copy this to your Azure App Service Configuration (App Settings)

# ============================================================================
# REQUIRED - Database Configuration
# ============================================================================
DATABASE_URL=postgresql+asyncpg://[username]@[server]:[password]@[server].postgres.database.azure.com:5432/[database]?ssl=require

# ============================================================================
# REQUIRED - Authentication
# ============================================================================
AUTH_SECRET_KEY=CHANGE_THIS_TO_RANDOM_32_CHAR_STRING
SESSION_TIMEOUT_HOURS=8
PASSWORD_MIN_LENGTH=8

# ============================================================================
# REQUIRED - Application Settings
# ============================================================================
APP_NAME=Secure Renewals API
APP_ENV=production
API_PREFIX=/api
LOG_LEVEL=INFO
ALLOWED_ORIGINS=https://[your-app-name].azurewebsites.net

# ============================================================================
# REQUIRED - Python Runtime
# ============================================================================
PYTHON_VERSION=3.11
SCM_DO_BUILD_DURING_DEPLOYMENT=true
ENABLE_ORYX_BUILD=true

# ============================================================================
# OPTIONAL - Email Configuration (for notifications)
# ============================================================================
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=hr@company.com
SMTP_PASSWORD=your-email-password
SMTP_FROM_EMAIL=hr@company.com
SMTP_FROM_NAME=Baynunah HR
SMTP_USE_TLS=true
APP_BASE_URL=https://[your-app-name].azurewebsites.net

# ============================================================================
# OPTIONAL - Build Configuration
# ============================================================================
POST_BUILD_COMMAND=cd frontend && npm install && npm run build && mkdir -p ../backend/static && cp -r dist/* ../backend/static/

# ============================================================================
# OPTIONAL - Development/Testing (DO NOT USE IN PRODUCTION)
# ============================================================================
# DEV_AUTH_BYPASS=false

# ============================================================================
# How to Set These Variables in Azure
# ============================================================================
# Method 1: Azure Portal
# 1. Go to Azure Portal: https://portal.azure.com
# 2. Navigate to your App Service
# 3. Click "Configuration" in the left menu
# 4. Click "New application setting" for each variable
# 5. Click "Save" when done
#
# Method 2: Azure CLI
# az webapp config appsettings set \
#     --resource-group [resource-group] \
#     --name [app-name] \
#     --settings KEY1=VALUE1 KEY2=VALUE2
#
# Method 3: During deployment script
# The deploy_to_azure.sh script sets most of these automatically
# ============================================================================
